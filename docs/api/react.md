# React API 参考

来自子路径导出：

```ts
import {
  ApiProvider,
  useApi,
  useApiCall,
  useBatchApiCall,
  useApiPolling,
  usePaginatedApi,
  useInfiniteApi,
  useMutation,
  useApiCleanup,
  useIntersectionObserver,
} from '@ldesign/api/react'
```

